{
    "tasks": [
        {
            "label": "Build CTDL-GT",
            "type": "shell",
            "command": "cmake --build build --parallel",
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Builds the CMake project."
        }
    ],
    "version": "2.0.0"
}